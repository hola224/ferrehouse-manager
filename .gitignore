node_modules/
dist/
*.local
.env
.env.*
!.env.example
*.db
*.db-journal
*.db-wal
*.db-shm

# La copia que aparta el restaurador antes de reemplazar la base:
# `ferrehouse.db.antes-de-restaurar-2026-07-31-130542` y sus `-wal`/`-shm`.
# NO la cubre `*.db-wal`, porque el sufijo va al final del nombre completo y
# no después del `.db`. Siete de estas se colaron al repositorio desde los
# tests antes de que existiera esta línea.
*.antes-de-restaurar-*
backups/

# Credenciales de la sesion de WhatsApp. Vincular de nuevo es escanear un QR;
# subirlas al repositorio es regalar la sesion de la tienda.
.wa-sesion/

# Worktrees y estado local de los agentes de Claude Code.
.claude/

# Papeles de trabajo del desarrollo. Viven solo en el PC de desarrollo: no van
# al repositorio publico ni al ZIP que baja el instalador.
.agents/
design_handoff_marca_ferrehouse/
BITACORA.md
CLAUDE.md
REVISION.md
SPRINTS.md
STATE.md
STATE UI.md
UI-BRIEF.md
USE-CASES.md

.DS_Store
*.tsbuildinfo
